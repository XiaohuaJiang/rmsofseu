<?php
App::uses('InterfaceController', 'Controller');

class AnyChatWebDemoInterfaceController extends InterfaceController {

/**
 * Myrobot view action. All necessary variables are set in the main interface controller.
 *
 * @return null
 */
	public function view() {
		// set the title of the HTML page
		$this->set('title_for_layout', 'Voice Control');
		// we will need some RWT libraries
		$this->set('rwt', array('voicecontroljs' => 'current'));
		
	}

}
